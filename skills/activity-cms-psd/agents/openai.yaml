display_name: Activity CMS PSD
short_description: Convert activity PSDs into activityincms JSON drafts.
default_prompt: Convert this activity PSD into an activityincms import package, choose or confirm an output directory, then produce assets, cms-page-config.json, theme.json, and theme.md. Use debug mode only when inspect files are needed.
