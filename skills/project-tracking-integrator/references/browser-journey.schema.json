{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/project-tracking-integrator/browser-journey.schema.json",
  "title": "Browser tracking verification journey",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "name", "startUrl", "environmentHost", "steps", "verification"],
  "properties": {
    "version": { "const": 1 },
    "name": { "type": "string", "minLength": 1 },
    "startUrl": { "type": "string", "format": "uri", "pattern": "^https?://" },
    "environmentHost": { "type": "string", "minLength": 1 },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "distinctId": { "type": "string", "minLength": 1 }
      }
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/step" }
    },
    "verification": {
      "type": "object",
      "additionalProperties": false,
      "required": ["contract", "sinceMinutes", "ingestionWaitSeconds", "maxQueryAttempts"],
      "properties": {
        "contract": { "type": "string", "minLength": 1 },
        "sinceMinutes": { "type": "integer", "minimum": 1, "maximum": 1440 },
        "ingestionWaitSeconds": { "type": "integer", "minimum": 0, "maximum": 60 },
        "maxQueryAttempts": { "type": "integer", "minimum": 1, "maximum": 2 },
        "consolePattern": { "type": "string", "minLength": 1 }
      }
    }
  },
  "$defs": {
    "locator": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "enum": ["testId", "dataAttribute", "href", "role", "label", "placeholder", "text", "css"]
        },
        "value": { "type": "string", "minLength": 1 },
        "role": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "attribute": { "type": "string", "minLength": 1 }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "role" } }, "required": ["type"] },
          "then": { "required": ["role", "name"] },
          "else": { "required": ["value"] }
        },
        {
          "if": { "properties": { "type": { "const": "dataAttribute" } }, "required": ["type"] },
          "then": { "required": ["attribute", "value"] }
        }
      ]
    },
    "expectation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "value"],
      "properties": {
        "type": { "enum": ["visibleText", "visibleElement", "url"] },
        "value": { "type": "string", "minLength": 1 }
      }
    },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["action"],
      "properties": {
        "action": { "enum": ["goto", "click", "fill", "select", "press", "expect-visible", "expect-url"] },
        "url": { "type": "string", "format": "uri", "pattern": "^https?://" },
        "locator": { "$ref": "#/$defs/locator" },
        "value": { "type": "string" },
        "expect": { "$ref": "#/$defs/expectation" }
      },
      "allOf": [
        {
          "if": { "properties": { "action": { "const": "goto" } }, "required": ["action"] },
          "then": { "required": ["url"] }
        },
        {
          "if": { "properties": { "action": { "enum": ["click", "fill", "select", "press", "expect-visible"] } }, "required": ["action"] },
          "then": { "required": ["locator"] }
        },
        {
          "if": { "properties": { "action": { "enum": ["fill", "select", "press"] } }, "required": ["action"] },
          "then": { "required": ["value"] }
        },
        {
          "if": { "properties": { "action": { "const": "expect-url" } }, "required": ["action"] },
          "then": { "required": ["value"] }
        }
      ]
    }
  }
}
